import { BaseInputParams, InputBindingPlugin } from '@tweakpane/core';
import { Thumbnail } from './controller.js';
interface PluginInputParams extends BaseInputParams {
    view: 'thumbnail-list';
    options: Thumbnail[];
}
/**
 * Thumbnail List Plugin
 *
 * `InputBindingPlugin<In, Ex, P>` means...
 * - The plugin receives the bound value as `Ex`,
 * - converts `Ex` into `In` and holds it
 * - P is the type of the parsed parameters
 */
export declare const TweakpaneThumbnailListPlugin: InputBindingPlugin<Thumbnail | null, string, PluginInputParams>;
export {};
